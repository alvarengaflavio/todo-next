import { FC } from "react";

interface AccountPageProps {}

const AccountPage: FC<AccountPageProps> = ({}) => {
  return (
    <section className="w-full text-center">
      <h2 className="text-2xl font-semibold mt-4">Conta</h2>
    </section>
  );
};

export default AccountPage;
